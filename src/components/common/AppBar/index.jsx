import React, { Component } from 'react';

export default class AppBar extends Component {
    render() {
        return (
            <header className="site-header dashed-bottom">
                <a className="site-title" href="#">Page Title</a>
            </header>
        );
    }
}
